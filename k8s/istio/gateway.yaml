apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: truxify-gateway
  namespace: truxify
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "truxify.com"
    - "*.truxify.com"
