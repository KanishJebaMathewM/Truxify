apiVersion: telemetry.istio.io/v1alpha1
kind: Telemetry
metadata:
  name: truxify-telemetry
  namespace: truxify
spec:
  metrics:
  - providers:
    - name: prometheus
  tracing:
  - providers:
    - name: jaeger
    randomSamplingPercentage: 50
